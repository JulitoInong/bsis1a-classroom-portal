BSIS 1-A PORTAL — FINAL PRE-DEPLOYMENT VALIDATION
Date: 2026-08-14

SOURCE / SCHEDULE
- Tuesday = current F2F.
- Monday, Wednesday, Thursday, Friday = online days.
- Saturday = separate NSTP 1 + PE-1 entries.
- No invented online subject/time entries.
- Regular/original enrollment schedule remains separate.

PRIORITY FIXES
- Google account switching signs out the current Supabase browser session using local scope.
- Only Supabase authentication storage is removed; unrelated local/session storage is preserved.
- Account-switch intent survives the redirect to the login page.
- Login does not auto-redirect to Registrar while account switching.
- Google OAuth uses prompt=select_account during account switching.
- Registrar clears the one-time account-switch intent only after a valid session is established.
- Production admin OAuth callback path is /admin/admin-callback.html.
- PWA shell cache version bumped to v3.
- Admin sign-out returns to the Student Portal login.

STATIC VALIDATION
- JavaScript syntax checks: PASS.
- Duplicate HTML IDs: NONE FOUND.
- Missing local HTML/CSS/JS/image references: NONE FOUND.
- No 127.0.0.1/localhost references in application HTML/JS/CSS/webmanifest.
- No localStorage.clear() or sessionStorage.clear() calls remain in application HTML/JS/CSS/webmanifest.

PRODUCTION CONFIGURATION
- Site URL: https://bsis1a-portal.netlify.app/
- Student OAuth redirect: https://bsis1a-portal.netlify.app/registrar/index.html
- Admin OAuth redirect: https://bsis1a-portal.netlify.app/admin/admin-callback.html
- Required Supabase migrations must already be executed in the live project.

LIVE TESTS REQUIRED
- Google login with account A.
- Registrar account-switch flow from A to B.
- Student registration and existing-student routing.
- Home, Schedule, Class Directory, Class Officers, Profile.
- PFP upload/update.
- Admin recognition and Admin login.
- Admin student search/filter/view/edit/delete.
- Delete confirmation and live database removal.
- Admin schedule CRUD.
- Admin sign-out.
- PWA installation from HTTPS production URL.
- Mobile and desktop live testing.

This package is statically validated. Live Supabase/Google/Netlify behavior must still be confirmed on the deployed domain because those external services cannot be fully verified from the ZIP alone.
